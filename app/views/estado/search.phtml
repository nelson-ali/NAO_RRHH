<?php use Phalcon\Tag; ?>

<?php echo $this->getContent(); ?>

<table width="100%">
    <tr>
        <td align="left">
            <?php echo $this->tag->linkTo(array("estado/index", "Go Back")); ?>
        </td>
        <td align="right">
            <?php echo $this->tag->linkTo(array("estado/new", "Create ")); ?>
        </td>
    <tr>
</table>

<table class="browse" align="center">
    <thead>
        <tr>
            <th>Id</th>
            <th>Estado</th>
         </tr>
    </thead>
    <tbody>
    <?php foreach ($page->items as $estado) { ?>
        <tr>
            <td><?php echo $estado->id ?></td>
            <td><?php echo $estado->estado ?></td>
            <td><?php echo $this->tag->linkTo(array("estado/edit/" . $estado->id, "Edit")); ?></td>
            <td><?php echo $this->tag->linkTo(array("estado/delete/" . $estado->id, "Delete")); ?></td>
        </tr>
    <?php } ?>
    </tbody>
    <tbody>
        <tr>
            <td colspan="2" align="right">
                <table align="center">
                    <tr>
                        <td><?php echo $this->tag->linkTo("estado/search", "First") ?></td>
                        <td><?php echo $this->tag->linkTo("estado/search?page=" . $page->before, "Previous") ?></td>
                        <td><?php echo $this->tag->linkTo("estado/search?page=" . $page->next, "Next") ?></td>
                        <td><?php echo $this->tag->linkTo("estado/search?page=" . $page->last, "Last") ?></td>
                        <td><?php echo $page->current, "/", $page->total_pages ?></td>
                    </tr>
                </table>
            </td>
        </tr>
    <tbody>
</table>
