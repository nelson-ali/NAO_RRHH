<?php use Phalcon\Tag; ?>

<?php echo $this->getContent(); ?>

<table width="100%">
    <tr>
        <td align="left">
            <?php echo $this->tag->linkTo(array("tipo/index", "Go Back")); ?>
        </td>
        <td align="right">
            <?php echo $this->tag->linkTo(array("tipo/new", "Create ")); ?>
        </td>
    <tr>
</table>

<table class="browse" align="center">
    <thead>
        <tr>
            <th>Id</th>
            <th>Tipo</th>
         </tr>
    </thead>
    <tbody>
    <?php foreach ($page->items as $tipo) { ?>
        <tr>
            <td><?php echo $tipo->id ?></td>
            <td><?php echo $tipo->tipo ?></td>
            <td><?php echo $this->tag->linkTo(array("tipo/edit/" . $tipo->id, "Edit")); ?></td>
            <td><?php echo $this->tag->linkTo(array("tipo/delete/" . $tipo->id, "Delete")); ?></td>
        </tr>
    <?php } ?>
    </tbody>
    <tbody>
        <tr>
            <td colspan="2" align="right">
                <table align="center">
                    <tr>
                        <td><?php echo $this->tag->linkTo("tipo/search", "First") ?></td>
                        <td><?php echo $this->tag->linkTo("tipo/search?page=" . $page->before, "Previous") ?></td>
                        <td><?php echo $this->tag->linkTo("tipo/search?page=" . $page->next, "Next") ?></td>
                        <td><?php echo $this->tag->linkTo("tipo/search?page=" . $page->last, "Last") ?></td>
                        <td><?php echo $page->current, "/", $page->total_pages ?></td>
                    </tr>
                </table>
            </td>
        </tr>
    <tbody>
</table>
